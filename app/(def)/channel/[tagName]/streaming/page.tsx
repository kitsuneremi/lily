import { Metadata } from "next"

export const metadata: Metadata = {
    title: 'streaimg tab',
    description: 'streaimg tab'
}

export default function Page() {

    return (
        <div>
            streaming
        </div>
    )
}