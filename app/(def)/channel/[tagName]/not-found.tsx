import { Metadata } from "next"

export const metadata: Metadata = {
    title: 'đéo có kênh này',
    description: 'đéo có kênh này'
}

export default function Page(){

    return <div>
        đéo có kênh này
    </div>
}